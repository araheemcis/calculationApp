apiVersion: v1
kind: Service
metadata:
  name: calculationapp2
spec:
  selector:
      app: calculationapp2
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
#      nodePort: 31134
#  type: NodePort

